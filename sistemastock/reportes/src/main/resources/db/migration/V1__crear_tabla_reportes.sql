CREATE TABLE reportes (
                          id BIGINT AUTO_INCREMENT PRIMARY KEY,
                          tipo VARCHAR(100),
                          fecha_generacion VARCHAR(50),
                          contenido TEXT
);