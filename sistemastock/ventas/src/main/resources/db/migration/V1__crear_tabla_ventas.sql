CREATE TABLE ventas (
                        id BIGINT AUTO_INCREMENT PRIMARY KEY,
                        cliente VARCHAR(255),
                        fecha VARCHAR(50),
                        total DOUBLE
);